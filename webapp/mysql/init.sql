CREATE TABLE dates (
    id int AUTO_INCREMENT PRIMARY KEY,
    date date,
);

CREATE TABLE laungages (
    id int AUTO_INCREMENT PRIMARY KEY,
    laungage varchar(255),
);

CREATE TABLE howlong (
    id int AUTO_INCREMENT PRIMARY KEY,
    time int,
);

CREATE TABLE contents (
    id int AUTO_INCREMENT PRIMARY KEY,
    content varchar(255),
);

